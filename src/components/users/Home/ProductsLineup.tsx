import React from 'react'

type ProductsProps = {
    title: string
}

const ProductsLineup = ({title}: ProductsProps): JSX.Element => {
  return (
    <div>
      
    </div>
  )
}

export default ProductsLineup
